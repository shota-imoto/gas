{
  "name": "gas",
  "private": true,
  "scripts": {
    "clasp": "clasp",
    "test": "vitest run",
    "setup": "bash scripts/setup.sh",
    "deploy": "bash scripts/deploy.sh"
  },
  "devDependencies": {
    "@google/clasp": "3.3.0",
    "vitest": "4.1.10"
  }
}
