# GASボット群 要件・進捗メモ

作成日: 2026-08-04
リポジトリ: `~/PJ/gas`

## 概要

複数のGoogle Apps Script(GAS)をこのリポジトリでgit管理し、GitHub Actionsでデプロイを自動化する。

現時点で予定しているプロジェクトは2つ。将来的にbot以外のGASもここに追加する可能性があるため、リポジトリ名は`gas-bots`ではなく`gas`(汎用)とした。肥大化したら非botのGASを別リポジトリに切り出すことも検討する。

| ディレクトリ | 内容 | 特性 | 状態 |
|---|---|---|---|
| `family-bot/` | 家計簿記入・共有カレンダー追加 | メッセージを受けて処理する双方向 → "bot" | 既存のGASプロジェクトあり(下記scriptId) |
| `band-bot/` | バンドのスケジュール調整依頼メッセージの投稿 | 一方向の通知のみ | 未着手(これから作成) |

("bot"という語は双方向のfamily-botにのみ使い、band-botは一方向通知だが名称としては便宜上bot表記を採用)

## 対象GASプロジェクト情報

### family-bot
- 紐づくスプレッドシート: https://docs.google.com/spreadsheets/d/REDACTED_SHEET_ID/edit
- scriptId: `REDACTED_SCRIPT_ID`
- コンテナバインド型(スプレッドシート紐づき)のため、スクリプト単体の共有設定はなく、スプレッドシートの共有権限がそのままスクリプトの権限になる

### band-bot
- まだGASプロジェクト自体が存在しない。方針が固まり次第、新規作成しこのリポジトリに追加する

## 方針(決定事項)

### git管理
Google公式CLI `clasp`でGASコードをローカルファイル化し、通常のgitリポジトリとして管理する。

### clasp導入方法
- グローバルインストールはローカルでの対話的な`clasp login`用にのみ使用(認証情報はユーザー単位で`~/.clasprc.json`等に保存されるため実害なし)
- リポジトリ側は`package.json`の`devDependencies`にバージョン固定(`3.3.0`)して導入し、実際の操作(ローカル・CI問わず)は`npx clasp ...`で統一する。CIでの再現性を優先する判断。

### デプロイ自動化
GitHub Actionsの`workflow_dispatch`(Actions画面からの手動実行)で`npx clasp push`/`deploy`を実行する(詳細は下部の補足参照)。
- `pull_request`トリガーは使わない(公開リポジトリで外部PRを受けた場合、Secretsがフォークに渡らないため)

### 認証・アカウント方針
CI用に専用のbotアカウント(`shota.imoto.dev@gmail.com`)を作成し分離する。
- 理由: CI Secrets漏洩時の被害範囲を専用アカウントに限定するため
- コストは個人Gmailなので無料(サービスアカウント+Workspace方式は有料のため不採用)
- botアカウントに対して、family-botのスプレッドシートを編集者として共有済み
- botアカウントでApps Script APIを有効化済み(script.google.com/home/usersettings)

## 進捗状況(このリポジトリでの作業)

- [x] `~/PJ/gas`を作成、`git init`(デフォルトブランチを`main`に変更)
- [x] `package.json`作成、`@google/clasp@3.3.0`をdevDependencyとしてインストール
- [x] `.gitignore`作成(`node_modules/`を除外)
- [x] `family-bot/`, `band-bot/`ディレクトリ作成(中身は空)
- [x] botアカウントで`clasp login`
- [x] `family-bot/`に`clasp clone <scriptId>`で既存GASコードを取得(`.clasp.json`は`family-bot/`直下に配置)
- [ ] band-botの新規GASプロジェクト作成(作成後、専用のGitHub Actionsワークフローも追加する)
- [x] GitHub Actionsワークフロー作成(`.github/workflows/deploy.yml`、`workflow_dispatch`手動実行、`npx clasp push`/`deploy`)
- [x] GitHubリポジトリ作成(公開): https://github.com/shota-imoto/gas
- [x] botアカウントの認証情報(`~/.clasprc.json`)をGitHub Secrets `CLASP_CREDENTIALS` に登録
- [x] git commit・push、ワークフロー動作確認済み(初回実行success)

## 補足: GitHub Actionsワークフローの設計メモ
- ワークフローファイルは`.github/workflows/deploy.yml`の1本のみ。ボットごとにファイルを分けない
- `push`トリガーによる自動デプロイはしない。`workflow_dispatch`(Actions画面からの手動実行)のみで、対象ディレクトリを`choice`入力で選択する方式にした
- ディレクトリを新規追加した場合(band-bot等)は、ワークフローの`inputs.project.options`に選択肢を1行追加する必要がある(手動実行前提なので許容)
- `clasp push`の後に`clasp deploy -i <固定deploymentId>`を実行し、**既存の1つのデプロイを更新し続ける**方式にした(Web AppのexecURLを固定するため)。理由:
  - Apps Scriptのバージョン付きデプロイには**上限20個**の制限があり、毎回新規デプロイ(`clasp deploy`をIDなしで実行)すると早々に上限に達する(実際にfamily-botで遭遇した)
  - Webhook(LINE等)にexec URLを登録する運用の場合、デプロイのたびにURLが変わると登録し直しが必要になり不便
  - 固定するdeploymentIdは**GitHub Secrets `DEPLOYMENT_IDS`にJSON(`{"family-bot":"...", "band-bot":"..."}`)として保存**している。`.clasp.json`には書かない(理由は下記インシデント参照)。ワークフロー内で`jq`により`inputs.project`をキーに値を取り出す
  - **重要**: 新規プロジェクト(band-bot等)の初回デプロイは`clasp deploy`(API経由)で作らないこと。**Apps Scriptエディタの「デプロイ」→「新しいデプロイ」からUIで手動作成する必要がある**(下記インシデント参照)。作成後に発行されたdeploymentIdを`DEPLOYMENT_IDS`シークレットにキーを1つ追加する形で登録する(`gh secret set DEPLOYMENT_IDS --repo shota-imoto/gas`でJSON全体を上書き)。以降の更新はCIの`clasp deploy -i <id>`でよい
- デプロイ後のWeb App URLはワークフロー側では一切出力しない(`GITHUB_OUTPUT`にも書かない)。理由: このリポジトリはPublicで、ログ・Step Summary・outputsはいずれも第三者が閲覧しうるため、execURL(=認証なしでPOST可能なエンドポイント)をそこに書くのは`.clasp.json`にハードコードするのと同じ漏洩経路になる(下記インシデント参照)。URLはデプロイのたびに変わらない(固定deploymentId方式)ので、そもそも都度確認する必要がない。同様に`clasp deploy --json`の出力(deploymentIdを含む)もログに出さないよう`/dev/null`に捨てている
- URLを確認したい場合は、Secretsは書き込み専用でGitHub側から読み出せないため、LINE Developersコンソール等の登録先控えを見るか、`clasp deployments`をローカルでbotアカウントとして実行して確認する
- **ロールバック・障害時対応について(許容・決定事項)**: 同じdeploymentIdを更新し続ける方式は、悪いデプロイが即座に本番Webhookに反映される(カナリアリリース等はない)リスクがある。ロールバック自体は`clasp deploy -i <deploymentId> -V <過去のバージョン番号>`で可能だが、デプロイ前のバージョン番号をワークフロー側で自動記録する仕組みは**あえて追加しないことにした**。
  - 理由: 家族間利用でトラフィックが限られており、問題が起きても「メンテ中だから使わないで」と伝えれば実害が小さいため、そこまでの作り込みは不要と判断
- CI実行時は`$HOME/.clasprc.json`にSecretsの中身を書き出してからclaspを実行している

## インシデント: 公開リポジトリへの実IDの誤コミット(2026-08-04)

family-botの初期コミットで、`.clasp.json`のdeploymentId(= `"access": "ANYONE_ANONYMOUS"`のWeb App execURLそのもの)、および`コード.js`内の`TARGET.sheetId`(家計簿スプレッドシート実ID)・`calendarId`(共有カレンダー実ID)を、Publicなリポジトリにそのままコミットしてしまっていた。

- **深刻だった点**: execURL(deploymentId)は認証なしで誰でも叩ける設定のため、これが公開されると第三者が家計簿・カレンダーに偽データをPOST可能な状態だった
- **対応**:
  1. 漏洩した旧deploymentIdは`clasp undeploy`で削除し、新しいdeploymentIdを発行
  2. 新deploymentIdは`.clasp.json`に書かず、GitHub Secrets `DEPLOYMENT_IDS`(JSON)に格納する方式に変更(上記参照)
  3. LINE側のWebhook URL登録も新execURLに更新(要ユーザー作業)
- **訂正**: 当初「LINE署名検証(`x-line-signature`)を追加する」方針にしようとしたが、**GASのWeb App(`doPost(e)`)はHTTPリクエストヘッダを一切取得できない仕様のため実装不可能**と判明。よって「execURLの秘匿性を唯一かつ妥当な防御とする」方針に確定した
- [x] ワークフローのログ・Step SummaryにdeploymentId/execURLが出力されないよう修正済み(`clasp deploy --json`の出力を`/dev/null`に捨て、Step SummaryにはURLを書かない。詳細は補足セクション参照)
- **残タスク(未対応)**:
  - [ ] `コード.js`の`TARGET.sheetId`・`calendarId`もScript Propertiesに移し、ソースコード・git履歴から実IDを排除する
  - [ ] 既に公開履歴に残っている旧ID(scriptId・旧sheetId・calendarId・旧deploymentId等)をgit履歴から消すかどうかは未検討。当面はリポジトリがPublicのままなので、少なくとも上記1点(Script Properties移行)を先に完了させる

## インシデント2: API経由の新規デプロイがアクセス不可になる(2026-08-04)

上記インシデント1の対応で、漏洩したデプロイを削除し`clasp deploy`(デプロイIDを指定しない新規作成)で作り直したところ、`appsscript.json`で`"access": "ANYONE_ANONYMOUS"`と設定しているにもかかわらず、匿名POSTがすべて**HTTP 403(アクセス権が必要です)**になった。

- Apps Scriptエディタの「デプロイを管理」画面で「アクセスできるユーザー」を「全員」に編集し直しても直らなかった
- 切り分けのため、既存の(以前からエディタUIで作られていた)別デプロイに同じリクエストを送ったところ200 OKで成功。新規APIデプロイのみが失敗することを確認
- **原因(推定)**: Apps Script API経由(`clasp deploy`の新規作成)で作ったWeb Appデプロイは、`appsscript.json`のアクセス設定が実際の配信レイヤーに正しく反映されない既知の制約がある。UI上で「全員」と表示・編集できても、実際のアクセス制御は反映されない
- **対処**: Apps Scriptエディタの「デプロイ」→**「新しいデプロイ」**(UIから手動)で作り直したところ正常に匿名アクセスできることを確認。壊れていたAPI製デプロイは削除し、`DEPLOYMENT_IDS`シークレットをUI製の新デプロイIDに更新した
- **教訓**: **新規プロジェクトの初回デプロイは必ずApps ScriptエディタUIから手動で作成すること**。CI(`clasp deploy -i <既存id>`)で行うのは、UIで一度作成済みのデプロイを更新する場合のみに限る

## 環境情報
- Node.js v20.5.1 / npm 9.8.0 / git インストール済み
- clasp グローバルインストール済み(v3.3.0、ログイン用)
